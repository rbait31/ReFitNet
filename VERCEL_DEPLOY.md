# 🚀 Деплой ReFitNet на Vercel

## 📋 Подготовка к деплою

### 1. Настройка базы данных

1. Убедитесь, что у вас есть база данных NeonDB (PostgreSQL)
2. Скопируйте connection string из NeonDB dashboard
3. Примените миграции к production базе:

```powershell
# Используйте production DATABASE_URL
$env:DATABASE_URL="your_production_connection_string"
npx prisma db push
```

Или через миграцию:
```powershell
$env:DATABASE_URL="your_production_connection_string"
npx prisma migrate deploy
```

**Важно:** Убедитесь, что в базе данных созданы все таблицы:
- `users`
- `accounts`
- `sessions`
- `verification_tokens`
- `results` (новая таблица для личного кабинета)

### 2. Настройка Google OAuth для Production

1. Откройте [Google Cloud Console](https://console.cloud.google.com/)
2. Перейдите в **APIs & Services** → **Credentials**
3. Найдите ваш OAuth 2.0 Client ID
4. В разделе **Authorized redirect URIs** добавьте:
   ```
   https://your-domain.vercel.app/api/auth/callback/google
   ```
   Замените `your-domain.vercel.app` на ваш реальный домен Vercel
5. Сохраните изменения и подождите 1-2 минуты

### 3. Генерация секретов для Production

Сгенерируйте новые секреты для production (НЕ используйте development секреты):

```powershell
# Генерация AUTH_SECRET и NEXTAUTH_SECRET
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

Скопируйте сгенерированную строку - она понадобится для обеих переменных.

### 4. Деплой на Vercel

#### Вариант 1: Через Vercel Dashboard (рекомендуется)

1. **Подключите репозиторий:**
   - Откройте [Vercel Dashboard](https://vercel.com/dashboard)
   - Нажмите "Add New Project"
   - Подключите ваш Git репозиторий (GitHub, GitLab, Bitbucket)

2. **Настройте проект:**
   - Framework Preset: **Next.js** (определится автоматически)
   - Root Directory: `.` (корень проекта)
   - Build Command: `prisma generate && next build` (уже настроено в `vercel.json`)
   - Output Directory: `.next` (по умолчанию)

3. **Добавьте переменные окружения:**
   В разделе "Environment Variables" добавьте все переменные из `vercel-env-variables.txt`:
   
   ```
   DATABASE_URL=your_production_neondb_connection_string
   GOOGLE_CLIENT_ID=your_google_client_id
   GOOGLE_CLIENT_SECRET=your_google_client_secret
   AUTH_SECRET=your_generated_secret
   NEXTAUTH_SECRET=your_generated_secret
   NEXTAUTH_URL=https://your-domain.vercel.app
   ```
   
   **Важно:**
   - `NEXTAUTH_URL` должен быть вашим production доменом (например: `https://refitnet.vercel.app`)
   - `AUTH_SECRET` и `NEXTAUTH_SECRET` должны быть одинаковыми (используйте сгенерированный секрет)
   - Используйте разные значения для production и development
   - После первого деплоя обновите `NEXTAUTH_URL` на реальный домен

4. **Деплой:**
   - Нажмите "Deploy"
   - Дождитесь завершения сборки
   - Проверьте, что деплой успешен

#### Вариант 2: Через Vercel CLI

1. **Установите Vercel CLI:**
   ```powershell
   npm i -g vercel
   ```

2. **Войдите в Vercel:**
   ```powershell
   vercel login
   ```

3. **Деплой:**
   ```powershell
   vercel
   ```
   
   При первом деплое:
   - Выберите проект (или создайте новый)
   - Добавьте переменные окружения через CLI или Dashboard

4. **Production деплой:**
   ```powershell
   vercel --prod
   ```

### 5. Настройка переменных окружения в Vercel

После деплоя убедитесь, что все переменные окружения установлены:

1. Откройте проект в Vercel Dashboard
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте все необходимые переменные:
   - `DATABASE_URL` - Production connection string от NeonDB
   - `GOOGLE_CLIENT_ID` - Из Google Cloud Console
   - `GOOGLE_CLIENT_SECRET` - Из Google Cloud Console
   - `AUTH_SECRET` - Сгенерированный секрет
   - `NEXTAUTH_SECRET` - Тот же секрет, что и AUTH_SECRET
   - `NEXTAUTH_URL` - Ваш production домен (например: `https://refitnet.vercel.app`)

4. **Важно:** Установите переменные для всех окружений:
   - Production
   - Preview
   - Development (если нужно)

5. **Перезапустите деплой** после добавления переменных:
   - Перейдите в **Deployments**
   - Найдите последний деплой
   - Нажмите "..." → "Redeploy"

### 6. Применение миграций базы данных

После первого деплоя примените миграции к production базе:

```powershell
# Установите Prisma CLI глобально (если еще не установлен)
npm install -g prisma

# Примените миграции
$env:DATABASE_URL="your_production_database_url"
npx prisma migrate deploy
```

Или используйте `db:push` для разработки:
```powershell
$env:DATABASE_URL="your_production_database_url"
npx prisma db push
```

### 7. Проверка работы

1. Откройте ваш домен Vercel
2. Перейдите на `/login`
3. Попробуйте войти через Google
4. Проверьте, что редирект работает корректно
5. Проверьте, что данные сохраняются в БД
6. Проверьте личный кабинет `/dashboard`
7. Проверьте создание результатов

## 🔧 Troubleshooting

### Ошибка: "Prisma Client not generated"

**Решение:**
- Убедитесь, что `postinstall` скрипт в `package.json` содержит `prisma generate`
- Проверьте, что `vercel.json` содержит правильный `buildCommand`
- Проверьте логи сборки в Vercel Dashboard

### Ошибка: "Database connection failed"

**Решение:**
- Проверьте, что `DATABASE_URL` правильно установлен в Vercel
- Убедитесь, что connection string включает `?sslmode=require`
- Проверьте, что база данных доступна из интернета
- Проверьте, что в NeonDB разрешены подключения из Vercel

### Ошибка: "OAuth callback error" или "redirect_uri_mismatch"

**Решение:**
- Проверьте, что в Google Console добавлен правильный redirect URI:
  `https://your-domain.vercel.app/api/auth/callback/google`
- Убедитесь, что `NEXTAUTH_URL` установлен правильно (должен быть полный URL с https)
- Проверьте, что `GOOGLE_CLIENT_ID` и `GOOGLE_CLIENT_SECRET` правильные
- Убедитесь, что в Google Console добавлены оба redirect URI (development и production)

### Ошибка: "Session not found"

**Решение:**
- Проверьте, что `AUTH_SECRET` и `NEXTAUTH_SECRET` установлены
- Убедитесь, что cookies работают (проверьте настройки браузера)
- Проверьте, что таблицы `sessions` и `users` созданы в БД
- Проверьте, что `NEXTAUTH_URL` установлен правильно

### Ошибка: "Table 'results' does not exist"

**Решение:**
- Примените миграции к production базе:
  ```powershell
  $env:DATABASE_URL="your_production_database_url"
  npx prisma db push
  ```
- Или используйте миграции:
  ```powershell
  $env:DATABASE_URL="your_production_database_url"
  npx prisma migrate deploy
  ```

### Ошибка сборки: "Module not found" или "Type errors"

**Решение:**
- Убедитесь, что все зависимости установлены (`npm install`)
- Проверьте, что TypeScript компилируется без ошибок локально
- Проверьте логи сборки в Vercel Dashboard
- Убедитесь, что все файлы закоммичены в Git

## 📝 Важные замечания

1. **База данных:**
   - Используйте production базу данных для Vercel
   - Не используйте development базу данных в production
   - Регулярно делайте бэкапы
   - Используйте connection pooling для NeonDB (добавьте `?pgbouncer=true` к connection string)

2. **Secrets:**
   - Никогда не коммитьте секреты в Git
   - Используйте разные секреты для development и production
   - Храните секреты только в Vercel Environment Variables
   - Регулярно ротируйте секреты

3. **Google OAuth:**
   - Используйте разные OAuth credentials для development и production (или добавьте оба redirect URI)
   - Регулярно проверяйте квоты API в Google Console
   - Убедитесь, что OAuth consent screen настроен правильно

4. **Мониторинг:**
   - Используйте Vercel Analytics для мониторинга производительности
   - Настройте логирование ошибок
   - Мониторьте использование базы данных
   - Настройте алерты в Vercel Dashboard

5. **Производительность:**
   - Используйте connection pooling для базы данных
   - Настройте кэширование где возможно
   - Мониторьте размер bundle (используйте `@next/bundle-analyzer`)

## 🚀 После деплоя

1. ✅ Проверьте все функции приложения
2. ✅ Протестируйте вход через Google
3. ✅ Проверьте создание и отображение результатов
4. ✅ Проверьте личный кабинет (`/dashboard`)
5. ✅ Проверьте публичные результаты (`/dashboard/public`)
6. ✅ Проверьте избранное (`/dashboard/favorites`)
7. ✅ Настройте кастомный домен (опционально)
8. ✅ Настройте мониторинг и алерты
9. ✅ Настройте автоматические бэкапы базы данных

## 📚 Дополнительные ресурсы

- [Vercel Documentation](https://vercel.com/docs)
- [Next.js Deployment](https://nextjs.org/docs/deployment)
- [Prisma on Vercel](https://www.prisma.io/docs/guides/deployment/deployment-guides/deploying-to-vercel)
- [NextAuth.js Deployment](https://next-auth.js.org/configuration/options#nextauth_url)
- [NeonDB Connection Pooling](https://neon.tech/docs/connect/connection-pooling)

## 📋 Чеклист деплоя

- [ ] База данных NeonDB создана и доступна
- [ ] Миграции применены к production базе
- [ ] Google OAuth настроен для production
- [ ] Redirect URI добавлен в Google Console
- [ ] Секреты сгенерированы для production
- [ ] Репозиторий подключен к Vercel
- [ ] Все переменные окружения добавлены в Vercel
- [ ] `NEXTAUTH_URL` установлен на production домен
- [ ] Деплой успешно завершен
- [ ] Вход через Google работает
- [ ] Личный кабинет работает
- [ ] Создание результатов работает
- [ ] Публичные результаты работают
